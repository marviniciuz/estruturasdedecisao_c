#include <stdio.h>

int main()
{
    int contador = 1;

    while (contador <= 5)// enquanto contador for menor ou igual a 5 continua contando.
    {
        printf("%i", contador);
        contador++;
    }
    
    return 0;
}